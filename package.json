{
    "displayName": "Homebridge RGBW MQTT",
    "name": "homebridge-rgbw-mqtt",
    "version": "0.0.2",
    "description": "Plugin to HomeBridge supporting RGBW (smart) light bulbs via MQTT",
    "main": "dist/accessory.js",
    "scripts": {
        "clean": "rimraf ./dist",
        "build": "rimraf ./dist && tsc",
        "prepublishOnly": "npm run build",
        "postpublish": "npm run clean",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Steffen Martin <steffen.martin.jena@web.de>",
    "engines": {
        "homebridge": ">=1.0.0"
    },
    "keywords": [
        "homebridge-plugin"
    ],
    "license": "Apache-2.0",
    "repository": {
        "type": "git",
        "url": "git://github.com/steffenmartin/homebridge-rgbw-mqtt.git"
    },
    "bugs": {
        "url": "http://github.com/steffenmartin/homebridge-rgbw-mqtt/issues"
    },
    "devDependencies": {
        "@types/node": "^14.14.31",
        "homebridge": "^1.0.4",
        "rimraf": "^3.0.2",
        "typescript": "^4.1.3"
    },
    "dependencies": {
        "mqtt": "^4.2.6",
        "tsc": "^1.20150623.0"
    }
}
